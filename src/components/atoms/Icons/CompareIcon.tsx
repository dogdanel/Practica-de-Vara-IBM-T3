import IconBase from './IconBase'
import type { IconProps } from './types'

function CompareIcon(props: IconProps) {
  return <IconBase name="arrow-right-arrow-left" {...props} />
}

export default CompareIcon
